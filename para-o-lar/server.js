const app = require('./src/app');
const PORT = process.env.PORT;

app.listen(PORT, () => console.log(`baby, alô? oi amor. tudo bem contigo? só liguei pra perguntar se foi divertido estar na porta ${PORT}`))